// import * as firebase from '@firebase/testing';
// import fs from 'fs';
// import moduleName from 'module'

// const projectID = process.env.REACT_APP_PROJECT_ID;
// const databaseName = process.env.REACT_APP_DATABASE_URL;
// const rules = fs.readFile('./firestore.rules', 'utf8');

// const adminDB = firebase.initializeAdminApp({
//   projectId: projectID,
//   databaseName
// });

describe('/users', () => {
  describe('create', () => {
    it('正しいデータであれば作成できる', () => {
      // const userID =
    });
  });
});
